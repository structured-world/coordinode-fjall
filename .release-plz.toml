[workspace]
# Disable cargo-semver-checks — we control versioning manually for this fork.
semver_check = false
# Create GitHub releases with changelog
git_release_enable = true
# Use conventional commits for changelog
changelog_update = true
# Fork tags use v-prefix (v4.0.0) to distinguish from upstream (3.1.1)
git_tag_name = "v{{ version }}"

[changelog]
# Skip: chore, ci, style, build, Merge (no user-visible impact)
# Keep: feat, fix, perf, refactor, test, docs (user-visible)
commit_parsers = [
    { message = "^chore", skip = true },
    { message = "^ci", skip = true },
    { message = "^style", skip = true },
    { message = "^build", skip = true },
    { message = "^Merge", skip = true },
    { message = "^docs\\(ci\\)", skip = true },
    { message = "^docs\\(copilot\\)", skip = true },
    { message = "^docs", group = "Documentation" },
    { message = "^feat", group = "Added" },
    { message = "^fix", group = "Fixed" },
    { message = "^perf", group = "Performance" },
    { message = "^refactor", group = "Refactored" },
    { message = "^test", group = "Testing" },
]
